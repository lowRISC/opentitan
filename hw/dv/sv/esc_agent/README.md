# Escalation agent

The escalation agent extends from the `dv_lib` agent classes.

## Description

This agent implements the escalation interface protocol.
It can be configured to behave as either an escalation sender or receiver.
For design documentation, please refer to the `alert_handler` [specification](../../../top_earlgrey/ip_autogen/alert_handler/README.md).
