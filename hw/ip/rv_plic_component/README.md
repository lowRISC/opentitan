# The core of rv_plic

The `rv_plic` module is templated and is generated with [`ipgen`](../../../util/ipgen/README.md).
However, some of its design doesn't need to be templated.
This directory holds the parts that are used by each instantiation.
